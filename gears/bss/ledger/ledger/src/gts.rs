//! Link-time GTS catalog contributed by the Billing Ledger gear.
//!
//! The [`permissions`] module declares the authorization permissions that match
//! the resource labels and actions enforced by [`crate::authz`].

pub mod permissions;

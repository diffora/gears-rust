<!-- CONFLUENCE_TITLE: [BSS]: Subscriptions — Decisions Log -->
<!-- Related: ./PRD.md | Owners: BSS Subscriptions team -->

# Subscriptions — Decisions Log

Decision IDs are `SUB-D-NN`. Autonomous decisions follow the pricing-gear pattern: adopted into
the docs immediately, **flagged for veto** until Product/Architecture confirms. Severity:
H (high — commercial/model shape), M (medium), L (low).

> **Confirmation status (2026-08-01, veto round — the gear's first): ALL of SUB-D-01…26 CONFIRMED
> per-item by the product owner.** The round ratified the five load-bearing **[H]** items —
> **SUB-D-06** (the immutable `orderingTenantId`), **SUB-D-07** (the money-free recurring split,
> which is also this gear's ratified position in the rating SB1 recurring-WHEN conflict),
> **SUB-D-19** (the per-component key), **SUB-D-20** (as-of resolution for late cuts), **SUB-D-21**
> (`lineKey` identity + emitter completeness) — the new M batch SUB-D-22…25 (incl. the amount-less
> one-time lane valued by Billing, and the never-ETF reason set with `nonpayment_exhausted` in it),
> **SUB-D-26 as decided** (the cohort does not carry across *any* cancel+new class — the open
> Product question is answered: monthly→annual upsell loses protection, disclosed), the legacy
> M batch SUB-D-01…05/08…18, and the numeric defaults as a set (7d grace; 90d dwell / draft-TTL /
> cumulative pause-per-term; 30/14/7/1 notices). Inline "flagged for veto" annotations in the
> decision bodies and slices are historical (accurate as of their decision dates); this note and
> the Status column govern. Joint qualifiers stay owed where noted: SB1 (rating retires or
> subordinates its period tick), the SUB-R6 `lineKey` fixture, SUB-P6 contract fields, SUB-P9
> anchor adoption, and the SUB-B7/B8 obligations on the unauthored Billing gear.
> **SUB-D-27** (the `billingAnchorPolicy` adoption, minted later the same day when rating T-D-33
> resolved SB1 this gear's way) was **CONFIRMED in the same-day SB1-resolution ballot** — nothing
> in this gear awaits veto.

## Status board

| ID | Sev | Decision | Status |
|----|-----|----------|--------|
| SUB-D-01 | M | Scheduled lifecycle intents: `cancelMode { immediate, end_of_term, at(date) }` + `resumeAt`, pending intents on the aggregate | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-02 | M | `updateQuantity` as a first-class transition with the plan-change envelope; increases immediate, decreases next-cycle by default | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-03 | M | Billing-only pause = `collectionPaused` posture on `active` (subscription attribute, collection-scoped) | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-04 | M | Ramps: Contracts authors the committed schedule; Subscriptions executes generated scheduled intents; no native schedule aggregate | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-05 | M | Activation date trio (booking / service / acceptance) as attributes + events; no new statuses | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-06 | H | Ordering tenant pinned at creation: transfer rebinds commercial axes, never the ordering/partition key | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-07 | H | Recurring split: Subscriptions emits a money-free period fact; rating prices the recurring line; Billing posts | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-08 | M | Mutation-type inventory completed: `renew`, `unschedule`, `pauseCollection`/`resumeCollection`, `confirmAcceptance`, `extendTrial` | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-09 | M | Secondary producer-event inventory named in design (intents, renewal/grace, notices, pause, quantity, conversion, quota) | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-10 | M | Entitlement check surface: bounded-staleness degraded mode (last-known-good ≤ budget, then fail-closed) | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-11 | M | `draft → cancelled` (void) edge added; draft is exitable without activation | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-12 | M | `collectionPaused` suspends renewal payment pre-check + grace entry for in-window renewals (term extension continues) | **DECIDED (autonomous) 2026-07-15 · CONFIRMED 2026-08-01** |
| SUB-D-13 | M | Term boundaries always resolve: `autoRenew = false` ends by a system-derived end-of-term `cancel` (`term_expired`); payment resolution after a grace-driven suspension backdates the new term to the old term end (anchor sequence preserved) | **DECIDED (autonomous) 2026-07-28 · CONFIRMED 2026-08-01** |
| SUB-D-14 | M | Renewal snapshot-ref re-resolution is **eligibility-first** (amended by the 2026-07-28 billing pass): non-grandfathered re-binds to the current row each renewal; grandfathered keeps its pinned generation (cohort carried forward) until `grandfatherUntil` passes — re-bind then fires at the next renewal (`EligibilityExpirySignal`, SUB-P6) | **DECIDED (autonomous) 2026-07-28, amended same day · CONFIRMED 2026-08-01** |
| SUB-D-15 | M | A manual `changePlan`/`updateQuantity` mid-ramp supersedes the remaining Contract-authored steps (voided + Contracts signalled); operator intent wins | **DECIDED (autonomous) 2026-07-28 · CONFIRMED 2026-08-01** |
| SUB-D-16 | M | Nonpayment `suspended` dwell: platform default 90 days (tenant-configurable; Contract ladder overrides), **resolved + snapshotted at suspension** (evaluated deadline — later config changes never move an in-flight one) → system `cancel` (reason `nonpayment_exhausted`); §4.3b revival bounded by the dwell | **DECIDED (autonomous) 2026-07-28 · CONFIRMED 2026-08-01** |
| SUB-D-17 | M | Renewal price-change notice input: a pricing-sourced flag (scheduled supersession / `grandfatherUntil` expiry at the renewal instant) arms the 30-day notice as a commercial notice; no money computed here | **DECIDED (autonomous) 2026-07-28 · CONFIRMED 2026-08-01** |
| SUB-D-18 | M | Mid-term cancellation money path: the in-flight period fact stands; ETF/refund/credit = Contracts-defined, Billing-materialised from `SubscriptionCancelled` (instant + mode + reason + contract ref); this gear computes no money | **DECIDED (autonomous) 2026-07-28 · CONFIRMED 2026-08-01** |
| SUB-D-19 | H | Recurring period fact is **per component**: key `(subscriptionId, billing period, lineKey)` with a per-component traceability tuple — aligns with rating's period-driven unit key; one fact per period could not represent plan + add-ons (closes REVIEW F-08-1) | **DECIDED (autonomous) 2026-07-28 · CONFIRMED 2026-08-01** |
| SUB-D-20 | H | A cut resolves **as of what it describes, never as of when it runs**: components enumerated over the period (effective-dated), `payerTenantId` = the payer in force at period start, the §4.3b revival's snapshot re-resolution evaluated as of the backdated term start; a conversion-charge suspension revives without a term-extension step (the term is already in force) | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |
| SUB-D-21 | H | `lineKey` = the component **interval** (`plan#n` / `addon:{addOnId}#n`, lifetime interval ordinal); one fact per interval overlapping the period; an interval opened mid-period gets a targeted cut in its opening commit; rating derives coverage from the effective-dated read model (early-closed intervals under-cover, never overcharge); terminal-before-cut periods still cut for the served stretch; trial-phase periods cut normally; the quota-cycle reset re-keys to the period | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |
| SUB-D-22 | M | Dwell deadline lifecycle: voided by payment resolution (the §4.3b signal) or any committed exit from `suspended`; re-resolved from the acquiring payer's ladder on transfer (anchored at the original suspension); a Payments dispute on the blocking charge holds the clock; the terminal cancel is re-derived daily and its deprovision leg is a no-op against already-deprovisioned resources | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |
| SUB-D-23 | M | Firing-failure taxonomy gains a third class: **parked (state-precondition)** — the firing deadline is suspended while a named aggregate state blocks the firing and re-arms on the unblocking commit; window-end `resumeCollection` and boundary `convertTrial` against `suspended` are the canonical members | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |
| SUB-D-24 | M | One-time/setup charge lane (T-D-18 adoption formalised): this gear emits an **amount-less** `BillableItemCreated(kind=one_time)` at the qualifying instant, idempotent per `(subscriptionId, priceId)` for the subscription lifetime; **Billing values it from the frozen `pricingSnapshotRef`** (new seam SUB-B8) — no money computed here | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |
| SUB-D-25 | M | ETF path is reason-aware: only `customer`/`operator` cancels are early-termination class; `term_expired`, `nonpayment_exhausted`, `saga_superseded` never derive an ETF; `SubscriptionCancelled` additionally carries the current term window + billing-period identity as Billing's join key | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |
| SUB-D-26 | M | The grandfathered cohort does **not** carry across a cancel+new (`supersedesSubscriptionId`) pair — the successor is a fresh purchase gate; the loss of price protection MUST be disclosed on the change preview before execution (same doctrine as credit-forfeiture disclosure) | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |
| SUB-D-27 | M | `billingAnchorPolicy` adopted **verbatim** (pricing K2 enum + D-20 no-drift month-end clamp), executed by the emitter's period derivation (SUB-P9 unparked — SB1 resolved this gear's way, rating T-D-33/34); an anchor-altering plan change takes effect at the **next** period boundary; the K5 joint anchor fixture (calendar geometry ≡ fact identity) is a design-freeze gate | **DECIDED (autonomous) 2026-08-01 · CONFIRMED 2026-08-01** |

## Decisions

#### SUB-D-01 [M] Scheduled lifecycle intents (cancel at term end / at date; scheduled resume)

- **Where**: [PRD](./PRD.md) §6.1 `fr-scheduled-intents`, §6.5 interaction, AC 21–22.
- **Problem**: `changeMode` existed only for `changePlan`; "cancel at period end" (the most common self-service intent; Stripe `cancel_at_period_end`, Zuora cancellation policies, the predecessor's own glossary "Cancellation Policy") and "suspend until date" were unrepresentable — portal-side cron hacks would be invisible to the renewal job, Billing, and audit.
- **Options**: (a) pending-intent envelope on the aggregate reusing the §6.3 mode vocabulary; (b) status quo (portal automation).
- **Decision**: **(a), 2026-07-15** — `cancelMode ∈ {immediate, end_of_term, at(date)}`, `resumeAt` on suspend; pending intents stored on the aggregate, suppress renewal + next-term recurring, un-schedulable until effective, evented both ways; the effective-instant transition runs the full §6.1 guard set. *Flagged for veto.* **Propagated**: PRD §6.1 FR + §5.1 row + glossary (Cancellation policy) + AC 21–22 + §15 manifest-envelope row; gap analysis G-1 → actioned.

#### SUB-D-02 [M] `updateQuantity` as a first-class transition

- **Where**: [PRD](./PRD.md) §6.3 `fr-update-quantity`, §6.1 type list, AC 23; source: gap analysis G-3.
- **Problem**: pricing D-18 (`quantitySource = subscription_seat_count`) makes this gear the seat-count supplier, but the transition inventory had no quantity operation — no Policy gate, no proration boundary, no provenance for the number rating reads (Stripe `items.quantity`, Zuora `UpdateProduct` both first-class).
- **Options**: (a) full transition with the §6.3 envelope; (b) attribute-with-audit only (next-cycle effect).
- **Decision**: **(a), 2026-07-15**, with the up/down asymmetry defaulting to (b)'s conservatism: **increases MAY be immediate** (prorated by rating at the boundary), **decreases default `next-cycle`**. Seat counts consumed by rating MUST originate from committed transitions only. Policy-gated when quota-bearing; composition-changing event under AC 11 rules. *Flagged for veto.* **Propagated**: PRD §6.3 FR + §6.1 type list + §5.1 row + AC 23 + §15 manifest-type row; gap analysis G-3 → actioned.

#### SUB-D-03 [M] Billing-only pause = `collectionPaused` posture on `active`

- **Where**: [PRD](./PRD.md) §6.4 `fr-collection-pause`, glossary (Subscription pause), AC 24; source: gap analysis G-4.
- **Problem**: suspension always touches service (entitlement freeze, OSS pause); the inverse — service running, collection paused (hardship/dispute/goodwill; Stripe `pause_collection`; the predecessor's "Subscription Pause" P1 scope) — was unrepresentable without abusing `suspended`.
- **Options**: (a) subscription **attribute posture** on `active` (rides subscription events and renewal logic; reuses the §6.5 recurring-pause mechanism); (b) Billing-side AR hold (outside the aggregate).
- **Decision**: **(a), 2026-07-15** — `collectionPaused` auditable window (start/end/limit/reason) on `active`; collection-scoped only (service, entitlements, renewal evaluation untouched unless a separate intent says otherwise); Billing chooses the artifact treatment per policy. Pause-day limits and resume-proration mechanics stay open (§15) with Product/Billing. *Flagged for veto — Finance may still prefer (b); the aggregate-posture shape keeps the audit trail on the subscription either way.* **Propagated**: PRD §6.4 FR + §5.1 row + glossary + AC 24 + §15 mechanics row; gap analysis G-4 → decided.

#### SUB-D-04 [M] Ramps are Contract-authored; Subscriptions executes scheduled intents

- **Where**: [PRD](./PRD.md) §6.3 `fr-ramp-execution`; source: gap analysis G-5.
- **Problem**: committed multi-step growth (Zuora Ramps/Orders, Stripe schedules) had no representation; a native schedule aggregate would duplicate Contracts' role as the home of negotiated commitments (cf. rating T-D-14 pools).
- **Options**: (a) Contracts authors the committed ramp, materialized as a sequence of scheduled `changePlan`/`updateQuantity` intents executed here; (b) native `SubscriptionSchedule` aggregate; (c) status quo (operator re-keys).
- **Decision**: **(a), 2026-07-15** — depends on the SUB-D-01/02 envelopes; atomic multi-action submission is a Contracts/Design follow-up (§15). (b) revisited only if self-service ramps become a product goal. *Flagged for veto.* **Propagated**: PRD §6.3 FR + §15 Contracts-follow-up row; gap analysis G-5 → decided.

#### SUB-D-05 [M] Activation date trio as attributes; no new statuses

- **Where**: [PRD](./PRD.md) §6.1 `fr-activation-instants`, AC 25; source: gap analysis G-6.
- **Problem**: `activate` was a single instant; Zuora's `ContractEffectiveDate` / `ServiceActivationDate` / `CustomerAcceptanceDate` distinction — which the ASC 606 hooks need for enterprise acceptance clauses — was collapsed.
- **Options**: (a) three instants as attributes/evaluated fields + an optional acceptance-confirmation operation; (b) manifest enum extension (pending-activation / pending-acceptance interim statuses); (c) purely Contracts/Finance-side dates.
- **Decision**: **(a)+(c) split, 2026-07-15** — `contractEffectiveAt` referenced from Contract (booking semantics stay Contracts/Finance SoR), `serviceActivatedAt` stamped at the activate commit, `customerAcceptedAt` stamped by an acceptance confirmation where Contract clauses require (else = service activation); all three ride lifecycle events and ASC hooks. (b) rejected — the manifest enum stays closed, `draft` covers pre-activation. Confirmation-flow shape → Design (§15). *Flagged for veto.* **Propagated**: PRD §6.1 FR + AC 25 + §15 acceptance-flow row; gap analysis G-6 → decided.

#### SUB-D-06 [H] Ordering tenant pinned at creation (transfer never rebinds the ordering key)

- **Where**: [PRD](./PRD.md) §6.7 `fr-event-ordering`, AC 26; design slices 01 §4.2, 07 §4.4; seam SUB-R1.
- **Problem**: the ordering/partition key is `(resourceTenantId, subscriptionId)` and storage is partitioned by `resourceTenantId`; an ownership transfer that rebinds `resourceTenantId` would switch the key mid-stream — pre/post-transfer events land in different partitions, rating's ordered consumption breaks exactly at the most sensitive moment, and the aggregate row set (revisions, intervals, entitlements, audit) would need a physical partition migration. The design review (2026-07-15) flagged this as its top finding.
- **Options**: (a) pin an immutable `orderingTenantId` at creation (= `resourceTenantId` at creation) used for ordering + partitioning; transfer rebinds the commercial axes only; (b) ordering-epoch/barrier protocol that re-keys the stream transactionally; (c) forbid `resourceTenantId` transfer (cancel+new only).
- **Decision**: **(a), 2026-07-15** — `orderingTenantId` is stamped at creation and immutable; the manifest ordering invariant reads onto it; `OwnershipTransferCompleted` carries both old and new axes **on the same partition**, so consumers re-key their own projections without a stream barrier. (b) rejected as a heavy protocol with no consumer that needs it; (c) rejected — transfer is a manifest §4.11 flow. *Flagged for veto.* **Propagated**: PRD §6.7 FR + AC 26; slice 01 §4.2, slice 07 §4.1/§4.4; SEAMS SUB-R1 note + ownership matrix.

#### SUB-D-07 [H] Recurring split: Subscriptions emits the period fact, rating prices it

- **Where**: [PRD](./PRD.md) §6.8 `fr-recurring-idempotency`, §9.2 billing handoff + rating read-model, §4 diagram, AC 27; design slice 08 §4.3; seams SUB-B1, SUB-R1.
- **Problem**: slice 08's `RecurringEmitter` sends `BillableItemCreated(kind=recurring)` straight to Billing, but this gear computes no money — while the rating PRD evaluates recurring components itself (flat per-period, `per_unit = unitPrice × seats`, hybrid emits recurring + usage as two lines). Two candidate producers of the recurring line ⇒ double-emission or an amount-less item Billing cannot post; no seam pinned the split.
- **Options**: (a) Subscriptions emits a **money-free recurring period fact** (period identity + traceability tuple + `pricingSnapshotRef`); rating consumes it as the period trigger, evaluates the recurring amount from the frozen snapshot, and emits the priced line to Billing; (b) Subscriptions prices recurring itself (violates WHEN/MATH); (c) rating self-triggers recurring off its own calendar (duplicates the anchor/pause/intent logic that lives here).
- **Decision**: **(a), 2026-07-15** — the WHEN/MATH split extended to recurring: this gear owns the period cut (anchor, pauses, pending intents, idempotency key `(subscriptionId, billing period)`); rating owns the price. The fact carries no monetary column; the priced line inherits the fact's idempotency key. *Flagged for veto — needs the rating counterpart contract updated (joint fixture).* **Propagated**: PRD §6.8 FR + §9.2 contracts + §4 diagram + AC 27; slice 08 §4.3; SEAMS SUB-B1/SUB-R1 + ownership matrix. **Amendments 2026-07-28**: the fact additionally snapshots **`payerTenantId` at cut time** (review #15 — old-payer default made representable) and carries the **suspended interval(s) + suspension-billing posture** (`pause_recurring | continue`; billing-pass review #3 — rating can now price a mid-period suspension while this gear still computes no money); slice 08 §4.3 + slice 09 §4.2/§4.3 carry the field rules. **Superseded in part by SUB-D-19 (2026-07-28):** the idempotency key named above gains a **component dimension** — `(subscriptionId, billing period, lineKey)`, one fact per billable component — because a subscription-wide key could not represent plan + add-ons nor be inherited by rating's per-component period-driven units; everything else in this decision (ownership of the cut, the money-free shape, inheritance) stands unchanged. Emission-vs-collection precedence pinned the same day: **grace suppression governs emission** (a grace-blocked next-term fact never emits during a pause window), **`collectionPaused` defers only the collection** of already-emitted facts — slice 04 §4.4 + slice 08 §4.3; emitted facts stay immutable and carry their suspension context.

#### SUB-D-08 [M] Mutation-type inventory completed

- **Where**: [PRD](./PRD.md) §6.1 `fr-transition-request`, §9.1 table; design slices 01 §3.1/§4.2, 09 §4.1; seam SUB-N1.
- **Problem**: the PRD's own FRs mandate mutations that had no `TransitionRequest.type`: manual **renew** (§6.5 "requires an explicit TransitionRequest"), **un-scheduling** a pending intent (SUB-D-01, AC 22), the `collectionPaused` set/clear (SUB-D-03), **acceptance confirmation** (SUB-D-05), **trial extension** (§6.10) — breaking the "single commit path, all mutations are TransitionRequests" principle; §9.1 also lagged behind `updateQuantity`/`convertTrial`/`transfer`.
- **Options**: (a) extend the type list with `renew`, `unschedule`, `pauseCollection`, `resumeCollection`, `confirmAcceptance`, `extendTrial` (same pending-manifest-alignment posture as `updateQuantity`/`convertTrial`); (b) model them as untyped admin endpoints outside the envelope.
- **Decision**: **(a), 2026-07-15** — every mutation gets a type; `extendTrial` is approval-gated (high-risk pattern), `unschedule` references the pending intent it voids. (b) rejected — it recreates the side door the Foundation exists to close. *Flagged for veto.* **Propagated**: PRD §6.1 type list + §9.1 table + §15 manifest row; slices 01/09; SEAMS SUB-N1. **Amendment 2026-07-28 (review fix)**: **`archive`** (the `cancelled → archived` retention edge, slice 01) joins the completion set — slice 01 defined it but every other inventory had missed it; now in PRD §6.1 list + §9.1 table + §15 manifest row, SEAMS SUB-N1, slice 09 §4.1, and the slice 08 producer registry (`SubscriptionArchived`). **Creation** is also pinned (slice 01 single-commit-path §): `create` = the constructor commit, tenant-scoped dedup key, `version = 0`, `SubscriptionCreated` seeds the ordering stream.

#### SUB-D-09 [M] Secondary producer-event inventory named in design

- **Where**: design slice 08 §4.1 (naming registry); [PRD](./PRD.md) §6.7 pointer; ACs 7/14/17/19/22/23/24.
- **Problem**: the §6.7 producer inventory repeats the manifest baseline, but PRD FRs mandate auditable events with no names anywhere: intent schedule/un-schedule (AC 22), renewal outcome + grace entry/exit (AC 7), notice triggers (AC 19), pause window (AC 24), the quantity composition event (AC 23, "naming per Design"), the first-class conversion event (AC 17), quota warning/exhausted/restored (AC 14). Slice 08 deferred the field matrix to slice 09, slice 09 deferred to "Design" — a circular deferral with no owner.
- **Options**: (a) slice 08 owns the full naming registry + required-context groups; slice 09 owns only wire mappings; (b) keep deferring to implementation PRs.
- **Decision**: **(a), 2026-07-15** — the secondary set is named normatively in slice 08 §4.1 (`SubscriptionIntentScheduled`/`…Unscheduled`, `SubscriptionRenewalSucceeded`/`…Failed`, `SubscriptionGraceEntered`/`…Exited`, `SubscriptionRenewalNoticeDue`, `SubscriptionCollectionPaused`/`…Resumed`, `SubscriptionQuantityChanged`, `SubscriptionTrialConverted`/`…Extended`/`…Expired`, `SubscriptionAcceptanceConfirmed`, `EntitlementQuotaWarning`/`…Exhausted`/`…Restored`). *Flagged for veto.* **Propagated**: slice 08 §3.3/§4.1; slice 09 §3.3; PRD §6.7 pointer sentence. **Amendment 2026-07-28 (review fix)**: (1) the ownership statement had survived contradictorily — slice 08 §3.3 still assigned the field matrix to 09 and slice 09 §1.1 still listed "event field matrices"; both now read **08 owns registry + field matrix, 09 owns wire mappings only** (the propagation pointer above should have named 08 §1.1/§3.3 + 09 §1.1). (2) Registry completed against its own completeness promise: `SubscriptionArchived` (primary set), `OwnershipTransferRejected` (approval denial + commit-time abort — a `Requested` consumer always learns the outcome), `SubscriptionTrialExtensionDenied`, `EntitlementFrozen`/`EntitlementUnfrozen` (the slice 05 `frozen` state, invisible to `Issued`/`Revoked` mirrors), `SubscriptionRampHalted` (`terminal_step_failure` | `superseded_by_manual_change`), and the normative `SubscriptionIntentUnscheduled.reason` enum (`operator`, `firing_failed`, `saga_compensated`, `superseded_terminal`, `superseded_by_manual_change`).

#### SUB-D-10 [M] Check surface: bounded-staleness degraded mode

- **Where**: design slice 05 §3.8/§4.3; [PRD](./PRD.md) §15 row; seam SUB-E3.
- **Problem**: slice 05 declared the point-of-use check "fails closed on read-model outage (never stale-allow)" while also being cache-first with a < 5s propagation baseline — caching *is* bounded staleness, so the posture was self-contradictory; and a hard fail-closed read path means one projection outage blocks every entitlement check platform-wide (availability catastrophe for a runtime read).
- **Options**: (a) explicit staleness budget: serve last-known-good decisions up to a bounded age (default 60s) on projection outage, then fail closed; transitions stay strictly fail-closed; (b) keep binary fail-closed; (c) fail-open indefinitely.
- **Decision**: **(a), 2026-07-15** — the budget makes the normal-mode cache and the degraded mode one coherent rule; the 60s default is a Product/OSS knob (§15). (b) rejected as an availability landmine; (c) rejected as a silent-overrun door. *Flagged for veto.* **Propagated**: slice 05 §3.8/§4.3; slice 09 §3.8 scoping (only the check surface carries this posture); PRD §15 budget row. **Amendment 2026-07-28 (review fix)**: the budget governs the **feature-flag dimension only** — the **quota** dimension fails closed to `blocking` beyond its own tighter **quota freshness bound** (provisional **10s**, Product/OSS, PRD §15) and never serves a last-known-good quota `allow` (slice 05 §4.3 flag-vs-quota split); the split is now propagated to slice 09 §2.2/§3.8 and the PRD §15 row, which had all still stated the blanket 60s rule.

#### SUB-D-11 [M] `draft → cancelled` (void) edge

- **Where**: [PRD](./PRD.md) §6.1 status machine + transitions table, AC 28; design slice 01 §4.1.
- **Problem**: the state machine had no exit from `draft` — abandoned drafts (incl. never-activated trials, whose expiry path is `cancel`) were immortal, and the single-commit-path principle forbids deleting them out-of-band.
- **Options**: (a) add `draft → cancelled` (void; immediate-only, not resource-affecting — nothing is provisioned, no OSS leg); (b) hard-delete drafts by retention job; (c) leave drafts forever.
- **Decision**: **(a), 2026-07-15** — void is a normal `cancel` from `draft` with the standard envelope and audit; an optional draft-retention TTL that *submits* the void is a Product knob (§15). Same manifest posture as `resume`: the manifest lists operations, not every arrow. *Flagged for veto.* **Propagated**: PRD §6.1 enum FR + table + diagram + AC 28 + §15 TTL row; slice 01 §4.1. **Amendment 2026-08-01 (wave-3 review, hardening)**: the TTL gains a **platform default — 90 days** (tenant-configurable; the Product knob overrides) on the SUB-D-13/16 argument: with no knob configured an abandoned draft — including a never-activated trial and a voided cancel+new successor — was immortal (the archive retention job touches only `cancelled`). A draft bills and provisions nothing, so the default is inventory hygiene, not revenue; slice 01 §4.1 carries it.

#### SUB-D-12 [M] `collectionPaused` suspends renewal collection, not renewal evaluation

- **Where**: [PRD](./PRD.md) §6.4 `fr-collection-pause`, AC 29; design slice 04 §4.2/§4.3; seam SUB-B2.
- **Problem**: SUB-D-03 left "renewal evaluation continues" during a pause — so an auto-renewal inside a hardship/dispute pause would run its payment pre-check, fail, enter grace, and suspend the customer: the collection-only pause would cause the exact service loss it exists to avoid.
- **Options**: (a) renewal evaluation and term extension continue, but the payment pre-check, grace entry, and dunning handoff are **suspended** for renewals whose collection falls inside the pause window — they run when the window ends (deferred collection per the Billing artifact treatment); (b) block renewal entirely during pause; (c) keep pre-check + grace running (status quo).
- **Decision**: **(a), 2026-07-15** — mirrors Stripe `pause_collection` semantics (service and term continuity, collection deferred); term extension stays deterministic for rating/Billing. (b) rejected — an expiring term during pause would kill service; (c) rejected as self-defeating. *Flagged for veto.* **Propagated**: PRD §6.4 FR + AC 29; slice 04 **§4.2 + §3.6 sequence branch (0b)** (pointer corrected 2026-07-28 — the register had claimed §4.2/§4.3 while §4.3 carried none of it; the executing §3.6 sequence now branches on the open window); SEAMS SUB-B2 note. **Amendment 2026-07-28 (review fix)**: the window-end executor is now named — an end-dated `pauseCollection` persists a derived `resumeCollection` `ScheduledIntent` fired by the `IntentScheduler` (slice 04 §4.2a: emits `SubscriptionCollectionResumed`, thaws a frozen ladder, triggers the deferred collection); a terminal state first voids the derived intent, closes the window, and the deferred collection of already-emitted facts still runs. **Second amendment (2026-07-28 billing pass, review #6)**: a `suspend` while a window is open is allowed — the window **survives the suspension**, and the derived window-end firing against a `suspended` aggregate parks **retryable** (not `guard_violation`/terminal) until `resume` or a terminal state; without the reclassification the window could never close and AC 29's deferred collection would die silently.

#### SUB-D-13 [M] Term boundaries always resolve: `autoRenew = false` expiry + post-suspension backdating

- **Where**: design slice 04 §3.6 (0a) / §4.3a / §4.3b; [PRD](./PRD.md) §6.5 `fr-renewal-evaluation`.
- **Problem** (2026-07-22 review findings #9/#10): a subscription created with `autoRenew = false` and no pending intent reached `endDate` with **no defined transition** (stay `active` unbilled? auto-cancel?), and the mainline suspended-for-nonpayment → pays → resumes flow had **no term semantics** — backdate, resume-start, and the re-run owner were all unstated (backdating was specified only for late success *inside* grace).
- **Options**: (a) `autoRenew=false` ends by a system-derived end-of-term `cancel` (`term_expired`) fired by the `RenewalJob`; payment resolution after a grace-driven suspension **backdates** the new term to the old term end via the same idempotent `(subscriptionId, currentTermSequence)` extension, then `resume` — anchor-derived period sequence unbroken; (b) linger `active` and let operators clean up; (c) start the post-suspension term at resume time (fresh anchor).
- **Decision**: **(a) (autonomous), 2026-07-28** — one backdating rule for grace and suspension alike; the suspended gap falls inside the new term with its billing governed by the explicit §4.1 suspension-billing policy (never an implicit charge); ordering = payment resolution → backdated extension → `resume`. (b) rejected — an unbilled immortal `active` is the exact "term ambiguity" the slice exists to kill; (c) rejected — a resume-time term start breaks slice 08 §4.3 anchor-derived period identity. *Flagged for veto.* **Propagated**: slice 04 §3.6 branch (0a) + §4.3a/§4.3b; PRD §6.5 FR; slice 01 §4.2 source-status matrix (`renew` from `suspended` = this path only).

#### SUB-D-14 [M] Renewal snapshot-ref re-resolution is eligibility-first (as amended)

- **Where**: design slice 04 §4.3; [PRD](./PRD.md) §6.5 `fr-renewal-evaluation`; DESIGN.md renewal sequence.
- **Problem** (2026-07-22 review finding #11): §3.6 and DESIGN.md said renewal success (the auto path — it is the `RenewalJob`) yields "fresh snapshot refs", while the PRD FR and §4.3 attributed fresh refs to **manual** renewal only — a commercial divergence (current published pricing vs frozen old price on the auto path).
- **Options**: (a) auto and manual alike refresh pricing-side refs; (b) auto renews at the frozen old refs forever.
- **Decision**: **(a) (autonomous), 2026-07-28 — AMENDED the same day (billing-pass review, Blocking #1)**: the original "unconditional refresh" inverted pricing's mechanism on both halves — the cohort's only carrier is the pinned price id (pricing design/07: "no separate binding store"), so replacing it destroyed generation selection, and pricing gates the re-bind on `grandfatherUntil` **expiry**, not on renewal (its window-sizing margin exists *because* re-bind happens at the next renewal after expiry). As amended: renewal re-resolution is **eligibility-first** — a non-grandfathered subscription re-binds to the current eligible row each renewal (supersessions propagate — the decision's original goal); a **grandfathered** subscription carries `priceEligibility` + `cohort` **forward** and keeps its generation until the generation's `grandfatherUntil` passes, the re-bind firing at the next renewal on pricing's `EligibilityExpirySignal` (new seam **SUB-P6**, incl. the outbound still-bound feedback pricing's backlog alarm consumes). Option (b) ("frozen refs forever") remains rejected — expiry-gated re-binding reaches supersessions without breaking cohorts. The `(currency, region)` segment persists (slice 02 §4.2). *Flagged for veto.* **Propagated**: slice 04 §4.3; slice 02 §4.2 sentence; PRD §6.5 FR; SEAMS SUB-P6 + register row; slice 09 §4.2 (signal + feedback); DESIGN.md sequence.

#### SUB-D-15 [M] Manual change mid-ramp supersedes the remaining Contract-authored steps

- **Where**: design slice 03 §4.5; slice 02 §4.1 future-interval rule (placeholder "§4.x" resolved 2026-08-01, wave-3 review #24h); slice 08 §4.1 (`SubscriptionRampHalted`, `SubscriptionIntentUnscheduled.reason`).
- **Problem** (2026-07-22 review finding #18): slice 02 allows future intervals to be voided by a "superseding change" without saying whether an operator's manual `changePlan` supersedes Contract-authored ramp intents; a manual B→C change mid-ramp left the next step (authored against B) to either revert the operator or halt unclassified.
- **Options**: (a) manual change **supersedes**: voids remaining steps (`superseded_by_manual_change`), signals Contracts (schedule owner) to re-author via `SubscriptionRampHalted`, executed prefix stands; (b) reject manual changes while a ramp is live.
- **Decision**: **(a) (autonomous), 2026-07-28** — operator intent wins and the outcome is evented, audited, and Contract-visible; (b) rejected as an operator lock-out with no emergency path. *Flagged for veto.* **Propagated**: S3 §4.5; S8 §4.1 registry; SUB-D-09 amendment (this document). *(Citation re-shaped to the resolver grammar 2026-08-01, wave-3 review #24h.)*

#### SUB-D-16 [M] Nonpayment `suspended` dwell — platform-default terminal step

- **Where**: design slice 04 §4.4; [PRD](./PRD.md) §6.5 grace policy defaults; [SEAMS](./SEAMS.md) SUB-C1 register row (unmappable "seam SUB-C1" target resolved 2026-08-01, wave-3 review #24h).
- **Problem** (2026-07-28 billing-pass review #11): the platform-default set covering the unauthored Contracts SoR (SUB-C1) stopped at the grace exit — `suspended → cancelled` was "only per contract-defined steps", so with no Contract a grace-exhausted subscription sat `suspended` forever: expired term, no renewal, no facts, `archive` unreachable (it applies to `cancelled`), §4.3b revivable indefinitely. The same immortal-state defect SUB-D-13 killed on the `active` side.
- **Options**: (a) platform-default dwell (90 days, tenant-configurable; Contract ladder overrides) then a system `cancel` (reason `nonpayment_exhausted`) through the commit path, §4.3b revival bounded by the dwell; (b) declare unbounded suspended inventory intended and give Finance a reporting surface.
- **Decision**: **(a) (autonomous), 2026-07-28** — symmetry with SUB-D-13 and with the 7-day-grace default doctrine (SUB-C1: platform defaults govern until Contracts authors); after the terminal step a returning customer is a new subscription. The effective dwell **resolves at suspension time** (Contract ladder → tenant → platform order) and persists as an **evaluated deadline field** on the aggregate — the §6.5(3) evaluated-fields discipline: later tenant/Contract changes govern *future* suspensions only and never move an in-flight deadline (the bounded terminal path stays bounded; an audited operator `cancel` remains available anytime). *Flagged for veto.* **Propagated**: S4 §4.4; PRD §6.5 defaults; S8 §4.1 (`SubscriptionCancelled.reason` enum, `nonpayment_exhausted`); SEAMS **SUB-C1** register row. *(Citation re-shaped to the resolver grammar 2026-08-01, wave-3 review #24h.)* **Refined by SUB-D-22 (2026-08-01)**: the deadline's lifecycle — voiding conditions, transfer re-resolution, dispute hold, and the daily re-derivation that makes the terminal step able to commit; "never moves" is narrowed there to configuration drift.

#### SUB-D-17 [M] Renewal price-change notice input

- **Where**: design slice 04 §4.5; SEAMS SUB-P6/SUB-F2; [PRD](./PRD.md) §16 risk row.
- **Problem** (2026-07-28 billing-pass review #9): notices are purely time-based and the SUB-D-14 re-resolution happens **at** renewal — after the last notice — while this gear computes no money, so a legitimate price-moving renewal (deliberate supersession; expiry-driven re-bind) could never be noticed in advance; the PRD's "non-compliant auto-renewals" risk had no implementable mitigation.
- **Options**: (a) a pricing-sourced boolean per bound row ("a scheduled supersession window or the bound generation's `grandfatherUntil` takes effect at/before the renewal instant") read from the pricing read model, arming the 30-day notice as a **commercial** notice (flag in the trigger payload; Notifications renders the variant; the amount, if wanted, is the Tariffs F-34 preview's job); (b) record pre-renewal price-change notification as out of scope and assign the obligation elsewhere.
- **Decision**: **(a) (autonomous), 2026-07-28** — the *fact* of a pending change is cheaply derivable from data pricing already publishes (scheduled `PriceWindow`s, `EligibilityExpirySignal` — SUB-P6); no money math enters this gear. *Flagged for veto.* **Propagated**: slice 04 §4.5; SEAMS SUB-P6 note + register row.

#### SUB-D-18 [M] Mid-term cancellation money path

- **Where**: design slice 08 §4.3; SEAMS **SUB-B7** (new); slice 08 §4.1 registry (`SubscriptionCancelled` payload); [PRD](./PRD.md) §6.8.
- **Problem** (2026-07-28 billing-pass review #10): the PRD requires "cancel mid-period → early-termination fee or refund per contract, materialized as Billing artifacts", but the design set never mentioned early termination or refunds, gave no disposition for an already-cut period fact when a cancel lands inside its period, and had no ownership row.
- **Options**: (a) the fact and any posted line **stand** (immutability; the period was contracted); the commercial consequence is **Contracts-defined and Billing-materialised** from `SubscriptionCancelled` (cancellation instant + `cancelMode` + reason + contract ref) as **new** artifacts; this gear computes no money; (b) retract/re-cut the fact on cancellation.
- **Decision**: **(a) (autonomous), 2026-07-28** — (b) violates the no-retro-edit invariant and re-introduces the double-producer hazard SUB-D-07 removed. *Flagged for veto.* **Propagated**: slice 08 §4.3 disposition rule; SEAMS SUB-B7; the `SubscriptionCancelled` payload fields (slice 08 §4.1 registry — "Where" pointer corrected 2026-08-01; the register had cited slice 01 §4.3, the scheduled-intents section). **Refined by SUB-D-25 (2026-08-01)**: the reason-eligibility class and the term/period join key.

#### SUB-D-19 [H] The recurring period fact is per component, not per subscription

- **Where**: design slice 08 §3.1/§3.6/§3.7/§4.3 + the recurring constraint; [PRD](./PRD.md) §6.8 `fr-recurring-idempotency`, §9.2 billing handoff, AC 5; SEAMS **SUB-B1**/**SUB-R6**.
- **Problem** (2026-07-28 cross-gear review): the fact was keyed `(subscriptionId, billing period)` with a **singular** traceability tuple `{subscriptionId, skuId, planId, priceId}`, while slice 02 models `AddOn` as a first-class effective-dated component carrying its own catalog keys — so a plan-plus-add-ons subscription had no representation. The mismatch is also cross-gear: rating synthesizes **period-driven units keyed `(subscription, priceId, chargeKind, lineKey, AnchorPeriod)`** (rating T-D-15), and SUB-D-07 requires the priced line to *inherit the fact's idempotency key*. A key with no component dimension cannot be inherited by N+1 component lines: either every component collapses into one line (wrong money on every add-on-bearing subscription) or several facts share one key and the "at most one per key" invariant — asserted in the slice constraint, §3.7's unique index, and AC 5 — breaks.
- **Options**: (a) add a **component dimension** to the key and the tuple: one fact per `(subscriptionId, billing period, lineKey)`, each carrying its own `{skuId, planId, priceId}`; (b) declare add-ons separately-keyed facts of a different kind (a second producer path); (c) keep one fact and let rating fan it out from the composition read model.
- **Decision**: **(a) (autonomous), 2026-07-28** — the recurring period fact is cut **per billable component**: key `(subscriptionId, billing period, lineKey)`, where `lineKey` identifies the component within the subscription's effective composition at the cut (the plan line and each `AddOn` line), and the traceability tuple becomes per component. `lineKey` is the **same coordinate rating carries in its period-driven unit key**, so SUB-D-07's inheritance rule holds unchanged — each priced line inherits its own component's key. Idempotency, the §3.7 unique index, and AC 5 all re-scope to the three-part key; the period-cut ownership (anchor, pauses, pending intents), the money-free shape, and the `payerTenantId`/suspension-interval carriage are untouched. (b) rejected — a second producer path is exactly the double-producer hazard SUB-D-07 closed; (c) rejected — it would make rating re-derive composition this gear already owns, and the fan-out would have no idempotency anchor. *Flagged for veto — the rating counterpart contract must adopt the same `lineKey` (joint fixture, seam SUB-R6 / rating SB1).* **Propagated**: slice 08 §3.1 (`RecurringBillableItem`, `TraceabilityTuple`), §3.6, §3.7 index, §4.3, the recurring constraint + traceability row; PRD §6.8 FR + §9.2 + AC 5; SEAMS SUB-B1/SUB-R6; REVIEW F-08-1 → fixed. **Refined by SUB-D-21 (2026-08-01)**: `lineKey`'s value, occurrence dimension, and stability rules are pinned there (the 2026-08-01 wave-3 review #8 found the coordinate descriptively defined on both sides of the seam).

#### SUB-D-20 [H] A cut resolves as of what it describes, never as of when it runs

- **Where**: design slice 04 §4.3b/§4.4; design slice 08 §4.3; design slice 06 §4.4; SEAMS SUB-P6 note; source: wave-3 review #1/#11/#13 ([reviews/2026-08-01](./reviews/2026-08-01-wave3-billing-domain-review.md)).
- **Problem** (2026-08-01 wave-3 review): §4.3b (backdated revival) and SUB-D-16 (90-day dwell) compose into cuts that run up to 90 days after the period they describe, and every as-of anchor was written as "at the cut"/"at cut time": (a) composition enumerated at the commit instant drops add-on intervals that ended inside the blocked period (never billed) or bills ones that had already ended; (b) `payerTenantId` snapshotted at commit lands a period consumed under the old payer on a transferee — defeating the field's purpose and the slice-07 in-flight default; (c) the revival's eligibility-first re-resolution evaluated at commit can run past pricing's `grandfatherUntil + one cycle` window margin, retroactively repricing an already-contracted period; (d) a suspension caused by a **failed trial conversion** has no never-succeeded renewal to re-run, so the mandated ordering (payment → backdated extension → resume) had no object and the customer could pay without service returning.
- **Options**: (a) pin every axis **as of the instant it describes**: components = the effective-dated composition **over the period** (deterministic whenever the cut runs — the past of the interval store is immutable), payer = the payer **in force at period start** (derived from committed transfer history), eligibility = evaluated **as of the backdated term start** (pricing's published windows are effective-dated and answer as-of reads); the §4.3b term-extension step applies **only when the blocked operation was a term extension** — a conversion-charge suspension's term is already in force, so its revival degenerates to payment resolution → `resume`; (b) resolve everything at commit time and forbid transfers/composition changes while suspended; (c) forbid revival cuts older than one period.
- **Decision**: **(a) (autonomous), 2026-08-01** — (a) is the only option under which the revival emits the same facts the un-suspended timeline would have emitted, which is the entire point of backdating; it needs no new state (composition and quantity are already effective-dated, transfers are committed history, pricing windows are as-of-readable). (b) rejected — it re-opens the immortal-suspended inventory SUB-D-16 closed and blocks legitimate transfers; (c) rejected — an arbitrary bound that reintroduces the never-billed-period defect for any dwell longer than one cycle. **Joint obligation (SEAMS SUB-P6 note)**: pricing's read surface must answer eligibility/window reads as of an instant up to the dwell bound (90d default) in the past — within its published-immutability model this is a read pattern, not new state. *Flagged for veto.* **Propagated**: slice 04 §4.3b (as-of ordering + the conversion-suspension branch + the margin note), §4.4 revival sentence; slice 08 §4.3 (payer axis + component enumeration re-anchored); slice 06 §3.6 revival note + §4.3 period-identity note; SEAMS SUB-P6 note; slice 07 payer-boundary note; PRD §6.8/§9.2 payer-axis wording + AC 32.

#### SUB-D-21 [H] `lineKey` identity and emitter completeness (mid-period, terminal, trial)

- **Where**: design slice 08 §3.1/§4.3; design slice 02 §4.1; design slice 03 §4.1/§4.2; design slice 05 §4.4; design slice 06 §4.4; [PRD](./PRD.md) §6.8 + AC 5; source: wave-3 review #2/#8/#9/#15/#16.
- **Problem** (2026-08-01 wave-3 review): SUB-D-19's coordinate was defined descriptively on both sides of the SUB-R6 seam (rating never defines `lineKey`; the joint fixture would be written against an unspecified field), had no occurrence dimension (an add-on removed and re-added inside one period maps both intervals to one key — the second fact hits the unique index and the stretch is never billed), and three emitter rules written for one-fact-per-period were never re-examined: no rule cut a fact for a component added mid-period ("Immediate ⇒ delta recurring" had no producer), no rule said whether a terminal state reached before the period's cut suppresses it or clips it, no rule said whether trial-phase periods are cut at all, and the slice-05 quota reset fired per cut instead of per period (a second component's cut granted a fresh mid-period allowance).
- **Options**: (a) `lineKey` identifies the component **interval**: `plan#n` for the n-th `PlanLink` interval, `addon:{addOnId}#n` for the n-th interval of that add-on — the ordinal is the 1-based sequence of the component's effective intervals over the subscription lifetime, assigned at interval open, immutable, stable across periods (a multi-period interval keeps its key; the unique index yields one fact per period per interval). The emitter cuts **one fact per component interval overlapping the period**; an interval **opened mid-period** (immediate `addAddOn`, in-place immediate `changePlan`) gets a **targeted cut in its opening commit**; `updateQuantity` opens no interval and rides the existing fact (rating prorates from `quantity @ t` + the boundary event, slice 02 §4.1 @-`t` discipline); an interval **closed early** after its fact was cut never retracts the fact — rating derives coverage from the effective-dated read model, so the early close under-covers automatically and never overcharges. **Terminal-before-cut**: the cut still runs for periods the term had begun serving (rating clips to the intervals the terminal commit closed — SUB-D-15/sweep rule); periods starting after the terminal instant are never cut. **Trial phases**: facts are cut normally — rating prices the phase from the frozen snapshot's phase chain (a free phase resolves to its zero-amount phase row; pricing owns that shape) and `convertTrial` moves the phase boundary on `SubscriptionTrialConverted` without opening a new period sequence. **Quota reset**: idempotent per `(subscriptionId, billing period)` — fired by the period's first cut, a no-op on every later component cut or re-cut of the same period. (b) key by catalog ids alone and forbid re-adding an add-on within a period; (c) let rating fan out sub-period coverage from one subscription-level fact.
- **Decision**: **(a) (autonomous), 2026-08-01** — the interval is the natural unit: it is what the composition store already models (slice 02: a change closes an interval and opens a new one), what rating slices at (slice 03 §4.1), and what makes the fact set deterministic from the period regardless of cut time (SUB-D-20). The load-bearing consequence: **the fact is the WHEN + identity trigger; coverage is derived** — rating resolves interval-in-force/`quantity @ t`/phase from the effective-dated read model over the period, which is immutable once past, so fact immutability and billing correctness stop competing. (b) rejected — it forbids a legitimate customer action to protect a key shape; (c) rejected by SUB-D-19 already (no idempotency anchor). *Flagged for veto — rating must adopt the same `lineKey` value rule into T-D-15's tuple and the SUB-R6/SB1 joint fixture (its docs currently use the field undefined); the trial-phase pricing shape (zero-amount phase row) rides pricing's phase chain (D-40/D-41).* **Propagated**: slice 08 §3.1 `lineKey` definition + §4.3 (mid-period cut, terminal precondition, trial-phase rule, reset note); slice 02 §4.1 interval-ordinal sentence; slice 03 §4.1/§4.2 (producer named for the immediate delta); slice 05 §4.4 reset re-key; slice 06 §4.4 conversion note; PRD §6.8 `fr-recurring-idempotency` + AC 5 (wave-3 #7 rides this decision); SEAMS SUB-R6 note.

#### SUB-D-22 [M] Dwell deadline lifecycle: clearing, transfer, dispute, and a cancel that can commit

- **Where**: design slice 04 §4.4 (+ §3.1 `NonpaymentDwell` field, §3.6 branch); design slice 01 §3.6 note; design slice 07 note; source: wave-3 review #3/#4.
- **Problem** (2026-08-01 wave-3 review): SUB-D-16 stored the dwell as an evaluated deadline that "never moves" with revival as the only counter-rule. Unhandled: (a) a customer who pays but whose `resume` fails closed on an overlap acquired during the suspension — or is resumed early by operator override — still gets terminal-cancelled (`nonpayment_exhausted`) while paid or active, and SUB-D-18 hands Billing an ETF trigger for it; (b) a transfer from `suspended` (legal once the ladder exhausted) carries the old payer's deadline to a payer whose own ladder differs, while the rule forbids re-resolving it; (c) a dispute raised after the suspension (the normal order) has no relief path — `pauseCollection` requires `active` and the ladder freeze needs a running ladder; (d) the terminal cancel's deprovision leg targets resources OSS already deprovisioned/paused at suspension, so the firing can exhaust `oss_unconfirmed` and land terminal with no committed status change — an immortal suspended state with an alarm instead of a fix, since a one-shot deadline never re-arms.
- **Options**: (a) scope the deadline to the **nonpayment-suspension episode**: it is **voided** by whichever comes first — payment resolution (the same signal that triggers §4.3b, so a paid-but-resume-blocked subscription is safe by construction), any committed exit from `suspended`, or its own firing; a **transfer re-resolves** it from the acquiring payer's ladder (ladder → tenant → platform) at transfer commit, **anchored at the original suspension instant** (SUB-D-16's "never moves" is narrowed to configuration drift; a payer rebind changes the deadline's source of authority and re-evaluates, audited — if the re-resolved deadline is already past, the terminal step fires right after the transfer commit, which the approving transferee accepted); a **Payments-declared dispute on the blocking charge** places a **dwell hold** — the clock stops for the dispute's duration and resumes on resolution (evaluated hold records; no service or collection effect); the terminal cancel is **re-derived daily** while the subscription remains `suspended` past its deadline (idempotent on `(subscriptionId, dwell episode)` — a standing system obligation, not a one-shot intent) and its **deprovision leg is a no-op** where the suspend's recorded OSS outcome already deprovisioned the resources (the firing consults the persisted work-order outcome; only still-provisioned/paused resources get an OSS leg); (b) keep the one-shot immovable deadline and route every edge to operator alarms.
- **Decision**: **(a) (autonomous), 2026-08-01** — every branch reuses existing machinery (the §4.3b payment-resolved signal, evaluated-fields replay, the recorded OSS outcome from the suspend, the daily job family) and the deadline keeps its purpose: it bounds the *episode*, not the customer. (b) rejected — it converts four reachable, benign sequences into revenue-affecting incidents by design. *Flagged for veto.* **Propagated**: slice 04 §4.4 (lifecycle rules) + §3.1 (`NonpaymentDwell` evaluated fields) + §3.6 (dwell branch); slice 01 §3.6 async note (no-op deprovision); slice 07 transfer note; PRD §6.5 default set + AC (wave-3 #24d).

#### SUB-D-23 [M] Firing-failure taxonomy: third class — parked (state-precondition)

- **Where**: design slice 01 §4.3 taxonomy + §4.2 matrix rows; design slice 04 §4.2a; design slice 06 §4.2 pointer; [PRD](./PRD.md) §15 (grace-horizon knob row); source: wave-3 review #5.
- **Problem** (2026-08-01 wave-3 review): SUB-D-12's second amendment and the `convertTrial` matrix row promise "parks until resume … or a terminal state", but the taxonomy they delegate to has exactly two classes and retryable is **bounded** — retries exhaust at `effectiveAt` + grace horizon and escalate to terminal. A nonpayment suspension can last the 90-day dwell, so for any suspension outlasting the horizon the promise cannot hold: the window-end firing dead-letters, the window never closes, AC 29's deferred collection dies with an alarm.
- **Options**: (a) add the class the two callers already assume: **parked (state-precondition)** — the firing is blocked by a *named* aggregate state (`suspended` for window-end `resumeCollection` and boundary `convertTrial`), the intent stays scheduled with suppressions held, the **firing deadline is suspended** while the blocking state persists, and the unblocking transition's commit **re-arms** the parked intents for immediate firing; parking is bounded by the blocking state's own lifecycle (the terminal sweep voids it; SUB-D-16/22 bound `suspended` itself), not by the grace horizon; (b) state that a parked window closes only by operator action or the terminal path.
- **Decision**: **(a) (autonomous), 2026-08-01** — the class is what the callers already promise and it is strictly safer than exhaustion-to-terminal: nothing is dropped, nothing needs an operator, and the bound still exists (the blocking state cannot outlive SUB-D-16/22). (b) rejected — it silently repeals SUB-D-12's second amendment. The **grace-horizon knob** the retryable class cites gets its owed PRD §15 row with this decision. *Flagged for veto.* **Propagated**: slice 01 §4.3 taxonomy (third class) + §4.2 matrix (`convertTrial`, `resumeCollection` rows cite the class); slice 04 §4.2a wording; PRD §15 row.

#### SUB-D-24 [M] One-time / setup charge lane (T-D-18 adoption formalised)

- **Where**: design slice 08 §3.1/§3.2/§3.7/§4.1/§4.3; design slice 09 §4.3 handoff; SEAMS **SUB-B8** (new); [PRD](./PRD.md) §6.8 FR + AC; source: wave-3 review #6.
- **Problem** (2026-08-01 wave-3 review): the T-D-18 adoption (rating synthesizes no unit for `one_time`/`one_time_setup`; this gear emits the billable at the qualifying instant) existed as two prose sites with no implementable surface: no registry row or field matrix, no emitting component, no store for the lifetime dedup key, no Billing handoff payload, no seam row, no SUB-D-xx record — and no owner for the **amount** (rating hands the charge to "Subscriptions/Billing" as a pair; neither gear had split it). The trials slice already routed the failed conversion charge into the grace ladder without naming an emitter.
- **Options**: (a) the fact is **amount-less**, like every artifact this gear emits: `BillableItemCreated(kind=one_time)` carrying the qualifying instant (activation, or trial conversion for a trialed plan), the per-component traceability tuple, and the frozen `pricingSnapshotRef`; **Billing values it from the ref** (copying a frozen flat amount is resolving a published price fact — no tier machinery exists on one-time rows by pricing's own rule); idempotent **once per subscription lifetime** per `(subscriptionId, priceId)` — a dedicated unique index, so re-activation, resume, plan change, and `PlanLink` migration never re-emit; (b) this gear stamps the amount onto the event at emission.
- **Decision**: **(a) (autonomous), 2026-08-01** — it keeps "no monetary column / computes no charge" absolute rather than recurring-scoped, and mirrors SUB-D-07's WHEN/MATH split (this gear = the qualifying instant + identity; the valuer = the gear that owns the artifact). (b) rejected — a second money-bearing producer path, and the first monetary field in this gear's store. **The Billing-side valuation obligation is recorded as seam SUB-B8** (Billing is unauthored — the platform default obligation pattern of SUB-C1 applies). *Flagged for veto — also closes the "who resolves the amount" half left open in rating DECISIONS T-D-18's hand-off to "Subscriptions/Billing".* **Propagated**: slice 08 §4.1 registry row (`kind=one_time` + field group), §3.2 `OneTimeEmitter`, §3.7 dedup index, §4.3 bullet (re-written to cite this decision); slice 09 §4.3 one-time handoff payload; SEAMS SUB-B8 + ownership row; PRD §6.8 `fr-onetime-emission` + AC 30.

#### SUB-D-25 [M] ETF derivation is reason-aware and gets a join key

- **Where**: design slice 08 §4.1 (`SubscriptionCancelled` payload) + §4.3 disposition bullet; SEAMS SUB-B7 note; [PRD](./PRD.md) §6.8; source: wave-3 review #10.
- **Problem** (2026-08-01 wave-3 review): SUB-D-18 hands Billing `SubscriptionCancelled` (instant + mode + reason + contract ref) to derive ETF/credit artifacts, but no site said which reasons are early-termination class — as written the platform can terminate a customer for nonpayment and then bill an ETF on top of §4.2a's deferred collection, charge an ETF for a term that ran to its natural end, or fee a saga-superseded predecessor whose "cancellation" is a plan change. And the payload carried no period/term identity, forcing Billing to re-derive the period from the aggregate at ETF time — the posting-time re-resolution the design eliminated on the payer axis, against a term a §4.3b extension may since have moved.
- **Options**: (a) pin the class here: only **`customer` and `operator`** cancels are ETF-eligible (Contracts defines amounts *within* the class); `term_expired`, `nonpayment_exhausted`, and `saga_superseded` **never** derive an ETF or unused-portion credit (natural end; collections/write-off path; plan-change continuity respectively) — and extend the payload with the **current term window (`termStartAt`/`termEndAt`) + the billing-period identity containing the cancellation instant**, so Billing joins against the period facts it already holds instead of re-reading the aggregate; (b) leave the class to Contracts entirely.
- **Decision**: **(a) (autonomous), 2026-08-01** — the never-ETF set is structural (it follows from what the reasons *mean* in this gear's own lifecycle), so leaving it to an unauthored Contracts SoR is the SUB-C1 anti-pattern: the platform default must be safe, and "fee the customer you just terminated for nonpayment" is not. (b) rejected accordingly. *Flagged for veto.* **Propagated**: slice 08 §4.1 payload fields + §4.3 disposition bullet; SEAMS SUB-B7 note; PRD §6.8 sentence + AC (wave-3 #24d).

#### SUB-D-26 [M] The grandfathered cohort does not carry across cancel+new; the loss is disclosed

- **Where**: design slice 03 §4.3 cancel+new mechanics; design slice 04 §4.3 note; [PRD](./PRD.md) §6.3; source: wave-3 review #12.
- **Problem** (2026-08-01 wave-3 review): SUB-D-14 carries `priceEligibility` + cohort forward across renewals because the pinned price id is the cohort's only carrier — but cross-currency/region/frequency changes execute as **cancel+new**, which mints a fresh purchase gate and pins a different price id, so a protected customer accepting a monthly→annual upsell silently loses price protection. Slice 05 records the quota-continuity consequence of the same handover and slice 02 the segment re-freeze; nothing recorded this one.
- **Options**: (a) the cohort does **not** carry across the `supersedesSubscriptionId` pair — the successor is a new subscription and a fresh gate (pricing's own framing: D-49 "changing country/currency = a new subscription"; `existing_grandfathered` selection is scoped to the subscription's pinned price id and the successor pins a new one — there is no carrier to extend); the **change preview/confirmation surface MUST disclose the loss of grandfathered protection before execution**, exactly as it already must for credit forfeiture; (b) build a cross-aggregate cohort carrier with pricing.
- **Decision**: **(a) (autonomous), 2026-08-01** — (b) is a pricing-model change this gear cannot make unilaterally and would contradict pricing's per-subscription eligibility scoping; what this gear *can* own is that the loss is never silent. If Product wants protection to survive specific cancel+new classes, that is a pricing-side decision to take at the veto round (noted in the flag). *Flagged for veto — includes the open question to Product/pricing: should any cancel+new class carry the cohort?* **Propagated**: slice 03 §4.3 (disclosure step in the saga preamble); slice 04 §4.3 note (grandfathered + cancel+new interaction); PRD §6.3 disclosure sentence. **Confirmed 2026-08-01 as decided — the open question is answered: no cancel+new class carries the cohort.**

#### SUB-D-27 [M] `billingAnchorPolicy` adopted verbatim; the emitter executes the clamp

- **Where**: [PRD](./PRD.md) §1.4 glossary row; S8 §4.3 period-key stability; S3 §4.1; SEAMS **SUB-P9** / **SUB-B1**; counterpart: rating T-D-33/T-D-34 + the K5 joint anchor fixture (pricing D-20).
- **Problem** (wave-3 review #20, parked as SUB-P9 pending SB1): pricing requires every recurring row to publish `billingAnchorPolicy` (K2 enum incl. the no-drift month-end clamp) and D-20 assigns the math's execution to this gear ("they execute the math"; the K5 fixture "exists before code") — but while rating's period tick also self-derived boundaries, the executor was ambiguous and the adoption was deliberately parked. Rating T-D-33 (2026-08-01) resolved SB1 this gear's way: the period fact is the recurring WHEN, rating's calendar is geometry/watchdog only — so the clamp's executable home is unambiguously **this gear's emitter**, and the parked adoption comes due (the wave-3 ranking predicted exactly this escalation).
- **Options**: (a) adopt the field **verbatim** like `prorationBasis` (glossary row, canonical enum owned by pricing, CI drift gate) — the emitter's period derivation (`RecurringEmitter`, slice 08 §4.3 period-key stability) computes `[periodStart, periodEnd)` from the frozen `billingAnchorPolicy` with the D-20 clamp (a Jan-31 monthly anchor bills Feb-28/29 and returns to the 31st — never permanent drift); an anchor-altering plan change takes effect from the **next** period boundary (the rule rating already asserts in the fixture — rating design/09 §4.3); the K5 joint anchor fixture asserts this gear's derived identity ≡ rating's calendar geometry and is a **design-freeze gate**; (b) keep deriving the anchor from Contract terms alone.
- **Decision**: **(a) (autonomous), 2026-08-01** — (b) leaves the catalog-published policy unexecuted anywhere (the wave-3 finding: "this gear would then implement a clamp it has never written down") and contradicts D-20's assignment. `billingAnchor` (the instant) stays derived from Contract terms at activation; the **policy** (how boundaries fall from it) is the adopted pricing field. *Confirmed 2026-08-01 (SB1-resolution ballot).* **Propagated**: PRD §1.4 glossary row (re-written from "parked"); S8 §4.3; S3 §4.1; SEAMS SUB-P9 + register row; rating design/09 §4.3 counterpart note.

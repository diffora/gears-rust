<!-- CONFLUENCE_TITLE: [BSS]: Rating — Decision Register -->
<!-- Related: ./DESIGN.md, ./PRD.md, ./SEAMS.md | Owners: BSS Rating team -->

# Rating — Decision Register

<!-- toc -->

- [Decision register](#decision-register)
- [Open items](#open-items)
- [Source](#source)

<!-- /toc -->

- [ ] `p1` - **ID**: `cpt-cf-bss-rating-decisions`

> Rating design decisions (rows T-D-01…T-D-15 were authored under the gear's historical name
> "Tariffs" — see T-D-16 / ADR-0002). The load-bearing early decisions are **inherited from the
> cross-gear seam analysis** with the pricing gear (2026-07-10) and recorded verbatim in
> [`SEAMS.md`](./SEAMS.md); this register is the gear-local index over them plus decisions taken
> during Design. Seam ids (K/O/S/W/M/P/F/B/C/G/N) resolve into [`SEAMS.md`](./SEAMS.md).

## Decision register

| # | Decision | Source seam | Status |
|---|----------|-------------|--------|
| T-D-01 | Adopt the pricing ten-axis canonical scope key for selection + non-overlap; `phase` is `phase_id`; multi-generation grandfathering selects by the pinned price id's `cohort`. **(2026-08-25: the canonical key is TEN axes since pricing D-196 — adoption is verbatim, so the adopted key is ten; slice-level `SelectionKey` absorption resolved the same day by T-D-35 — SelectionKey carries the full ten.)** | K1-K5, K6 | **adopted 2026-07-10; amended 2026-08-25** |
| T-D-02 | Step-4 overlays **stack** all survivors; class-specificity order breaks ties, not exclusivity. | O1-O3 | **adopted 2026-07-10 · pricing confirmed 2026-07-28** (design/09 `inst-plv-class-tiebreak` now states stack-all normatively — SEAMS O3 closed) |
| T-D-03 | One `pricingSnapshotRef`, **four writers** (registry, pricing, Subscriptions, Rating — "three writers" predated D-66's producer split and survived here alone while four neighbouring statements carried the correction, item 34 of the 2026-08-26 review); Tariffs is the composition SoR. | S1 | **adopted 2026-07-10** |
| T-D-04 | Open-period re-resolution replays strictly from the pinned snapshot (no live catalog read); tier-counter key `(subscription, meter, dimensionKey, window)`. | W2, M7 | **adopted 2026-07-10** |
| T-D-05 | `modelKind = {flat, per_unit, graduated, volume, package}`; per_unit, package, and composite/derived meter are in launch; Volume Variant B not authorable; hybrid/committed are compositions. | M1-M5 | **adopted 2026-07-10** |
| T-D-06 | Single pricing Slice 5 approval engine; Tariffs registers fail-closed validators, not a second workflow; ledger `dual_control` stays separate (different bounded context). | G1 | **adopted 2026-07-10** |
| T-D-07 | `prorationBasis` (incl. `none`) and `billingAnchorPolicy` + D-20 clamp adopted verbatim; enum-drift CI gate. | P1, P2 | **adopted 2026-07-10** |
| T-D-08 | Usage rows phase-invariant by default (phase-specific wins); reserved-rate two-source split; effective rev-share pass-through for bundle summing. | F1, M9, B1 | **adopted 2026-07-10** |
| T-D-09 | `pricingSnapshotRef` gains the **eighth named segment `commitmentReservation`** (reservation match: id, flavor, `reservedQuantity`, rate source; pool set: per-pool id, unit, `poolType`, balance @ `balanceVersion`, draw order, rollover, optional `overageRate` — the T-D-19 selector, restored to this row by the 2026-07-31 review #5; reserved-vs-pool split); writer Tariffs @ eval — closes the S1 segment-naming residue. | S1 | **adopted 2026-07-11** |
| T-D-10 | Commitment balance lifecycle: Rating publishes per-outcome `CommitmentBalanceEffect`s (idempotent on the outcome's evaluation/correction key); Contracts serializes per-pool `balanceVersion`; a balance-affecting correction cascades delta-only re-resolution of later-`balanceVersion` units that drew (or were rated overage against) the pool. | M8 → design/05 §4.1 | **adopted 2026-07-11** (cross-PRD: mirror in Contracts; the Rating side is intra-gear since T-D-16 — pipeline slice 15) |
| T-D-11 | Delta-dedup owner = **Rating** — persists delta outcomes by correction key; retried keys return the recorded outcome; Billing consumes Adjustments idempotently (defense in depth). | design/01 §2.2, design/08 §2.2 | **adopted 2026-07-11** |
| T-D-12 | Windowed models across intra-window boundaries: per-slice sub-window evaluation units with the frozen **`bandOffsetQ`**; window-activation/phase-conversion boundaries always carry (pricing `inst-tb-window-continuity`), plan-change per carry-vs-reset flag; volume selects the band by the **window total** (all slices re-resolve to the final total's band — clarified 2026-07-28); package counts blocks by cumulative ceil-diff; the correction key gains the slice coordinate (now `(unitKey[, slice], prior-rated-version, snapshot)` — generalized to period-driven units 2026-07-28). | M7, P2 → design/03 §4.3 | **adopted 2026-07-11** |
| T-D-13 | Step-6 recompute contract: a consumption-reservation split re-runs **steps 3–5 as a unit** over the on-demand remainder (re-band from zero; the frozen overlay stack + contract overlay re-apply); the reserved portion is not re-overlaid. Tier-counter exclusion + pool asymmetry (pricing `inst-rv-tier-q`) registered in PRD §6.6 / AC 19. | M9 → design/05 §4.2, design/04 §4.2 | **adopted 2026-07-11** |
| T-D-14 | Commitment pool flavors: frozen `poolType ∈ {prepaid_drawdown, committed_rate}` — billability per flavor (due-zero + notional lineage vs in-arrears at the committed rate) and normative shortfall true-up formulas (quantity/spend basis); the commitment sale itself bills outside PLAL (Contracts/Billing at sale). | M8 → design/05 §4.1/§4.5 | **adopted 2026-07-11** |
| T-D-15 | Period-driven evaluation units (recurring lines, capacity-flavor charges, true-up surfacing) keyed `(subscription, priceId, chargeKind, lineKey, AnchorPeriod)`, synthesized by **Rating's period tick** at anchor boundaries — no Tariffs scheduler; a zero-usage period still emits its period-driven units. | design/01 §4.2, design/11 §4.1 | **adopted 2026-07-11** |
| T-D-16 | **Gear consolidation**: Tariffs (evaluation core) and the Rating pipeline become **one `rating` gear** — the core as a no-I/O `rating-core` crate (evaluation slices 01–11 survive as-is), pipeline slices 12–16 absorb the empty rating-engine scope (VHP-810); "tariff" returns to the Pricing vocabulary (rate definitions); T-D-10/11/15 become intra-gear. Full naming + migration map: [`ADR/0002`](./ADR/0002-cpt-cf-bss-rating-adr-rating-gear-consolidation.md). | ADR-0002 | **accepted 2026-07-11** (single owner); commits A–B landed, residues tracked below |
| T-D-17 | **Level-based (gauge) aggregation via granule fold** (joint with pricing D-44, supersedes the F-40 "sum-only launch"): for `aggregationFunction ∈ {peak, time_weighted}` the meter consumes point-stamped gauge samples; the window cuts into frozen `aggregationGranularity ∈ {hour, day}` granules, each folds deterministically (max / step-integral with bounded `hold_last`, gap → 0 + operator signal, never a guess), and window `Q` = **sum of granule folds — additive by construction**, so M7 single-writer key, supersession continuity, T-D-12 `bandOffsetQ`, band/package math, and delta-only corrections are untouched; a late sample re-folds only its granule (qVersion++). Billable unit = level unit × granule duration (GB·h at `hour`, GB·day at `day` — the pricing D-77 pairing; the flat "level·granule-hours" gloss was exact only at `hour`, review #46), SKU-declared, distinct from the sample's level unit. No composite co-occurrence at launch. Product drivers: cloudlet peak-per-hour, storage GB-month. | pricing D-44; PRD `fr-level-aggregation` §6.5/§17.1; design/03 §4.3, design/12 §2.2, design/13 §4.1; SEAMS M10/UC6 | **adopted 2026-07-16 (product call, confirmed)** |
| T-D-18 | **`one_time` charges are not rated** (`one_time_setup` is withdrawn from the pricing contract — pricing D-375, 2026-09-20 — and the dedup rule below is amended by **T-D-36**) (closes the 2026-07-22 review finding #1): the pipeline synthesizes **no evaluation unit** for them — the three unit kinds stay `per_event` / windowed-`Q` / period-driven, and the period tick's synthesis set is exhaustive without them. They are billed **at their qualifying instant** (subscription activation; trial conversion for trialed plans) by **Subscriptions/Billing** from the frozen `pricingSnapshotRef` amount — the same at-sale path as the T-D-14 commitment sale; the dedup is owned there, and is **per phase-entry occurrence** rather than per subscription lifetime (T-D-36). The `chargeKind` axis keeps all **three** values **verbatim** (T-D-01/K3 — `recurring`, `usage`, `one_time`): one-time rows still resolve in step-2 selection for coverage, preview, and quote contexts — selection without unit synthesis. Coupons/discounts on one-time lines are a Billing-side concern (launch: none). *Autonomous — flagged for veto.* | PRD §5.2; design/01 §Domain (`EvaluationUnit`); design/02 §phase-semantics; design/14 §4.1; SEAMS K3 note | **adopted (autonomous) 2026-07-28 · confirmed 2026-08-01** |
| T-D-19 | Overage-rate selector: the frozen `commitmentReservation` segment MAY carry `overageRate` — present ⇒ the residual bills at that flat frozen rate; absent ⇒ banded on-demand rates over the **post-reservation remainder `Q`** (reservation excluded first, pool draws never deducted — disambiguated 2026-07-28 review; the earlier "full `Q`" contradicted §4.2 on a line carrying both a reservation and pools). The selector is the field's presence, frozen at contract publish. | design/05 §4.1 | **adopted (autonomous) 2026-07-28 · confirmed 2026-08-01** |
| T-D-20 | Coupon cross-scope exclusivity: under `exclusive_best`, a `line_total` candidate widens the exclusivity set to **one coupon per plan per period** — greatest total benefit wins, each candidate measured on its own scope; ties per the equal-benefit tie-break — the **open** Promotions joint item (design/06 §4.2; default proposal ascending `couponId`, applying identically to this cross-scope comparison). No `line_total` candidate ⇒ per-line exclusivity as before. | design/06 §4.2 | **adopted (autonomous) 2026-07-28 · confirmed 2026-08-01** |
| T-D-21 | Administrative re-rate = the one sanctioned exception to strict pin replay: a *policy* correction (corrective publish / historical import, always-material + two-person on the pricing side) replays over the **superseding** snapshot; input corrections stay strictly on the pin. | design/08 §4.x; PRD §6.10 sentence | **adopted (autonomous) 2026-07-28 · confirmed 2026-08-01** |
| T-D-22 | **`applyScope = line_total` fails closed at launch — no plan-scoped evaluation base exists.** Step 7 runs **per line** (design/06 §4.2) and every synthesized evaluation unit is sub-plan (`per_event` / windowed-`Q` slice / period-driven `(subscription, priceId, chargeKind, lineKey, AnchorPeriod)` — T-D-15/T-D-18); a hybrid plan's usage and recurring lines are **distinct units on distinct triggers** (usage arrival vs the period tick), so no point in the pipeline holds both pre-coupon amounts at once. `line_total` presumes exactly that (apply once plan-scoped, split back pro-rata), and T-D-20's cross-scope exclusivity presumes comparing benefits across those scopes. Launch rule: a `line_total`-scoped coupon snapshot **fails closed** at evaluation (never applied to one line, never guessed), and T-D-20's widening is **inert** while no `line_total` candidate can enter the set; per-line `exclusive_best` over `usage`/`recurring` scopes is unaffected. Defining the plan-scoped base (a plan-period assembly point, its trigger, and its re-resolution/cascade behaviour when a member line re-rates) is a **named joint gate with Promotions**, alongside the equal-benefit tie-break and mixed-settlement opens the same slice already fails closed on. | design/06 §4.2/§4.3; PRD §6.8, §17.2, §12 AC 16 | **adopted (autonomous) 2026-07-28 · confirmed 2026-08-01** (the fail-closed launch posture; the plan-scoped base itself stays the open joint gate with Promotions) |
| T-D-23 | **Reservation × sub-window slice band axis**: on a reservation-carrying line the banded axis is the **cumulative post-reservation remainder** (`remainderOffsetQ`, window-cumulative — T-D-19's basis); T-D-13's "re-band from zero" = the remainder axis excludes reserved quantity from the *window origin*, **never a per-slice reset** (a reset re-enters band 1 per split — the T-D-12 continuity argument); slice-13 materializes `remainderOffsetQ` beside `bandOffsetQ`; the step-4 stack does not reach the reserved portion (intended); joint fixture gains reservation + sub-window slice. Before this, T-D-12's `[bandOffsetQ, …)` and T-D-13's "from zero" gave two placements with no precedence — five figures apart per line on the worked-example bands. | 2026-07-31 billing-domain review #2/#41 → design/03 §4.3, design/05 §4.1/§4.2, design/04 §4.2, design/13 §4.3 | **adopted (autonomous) 2026-08-01 · confirmed 2026-08-01** |
| T-D-24 | **The pin of record advances on an administrative re-rate**: after a T-D-21 re-rate over superseding snapshot S′, later **input** corrections replay S′ and diff against the then-current rated version — a delta never embeds the negation of the two-person-approved repricing (the earlier "input corrections replay the original pin" did exactly that after any corrective publish). | review #3 → design/08 §4.1, design/14 §4.3 | **adopted (autonomous) 2026-08-01 · confirmed 2026-08-01** |
| T-D-25 | **Capacity-charge accrual basis**: `capacityCharge = reservedRate × reservedQuantity × coveredGranules` — `reservedRate` is denominated per billable-unit granule (level unit × granule duration), so the product is money per granule, not a period charge; the period emission multiplies by the reservation's covered duration in granules, summed per sub-interval on mid-period allocation/resize. Proration was previously undefined for it (recurring-only proration; usage never prorated). | review #6 → design/05 §3.6/§4.2, design/09 §3.6 | **adopted (autonomous) 2026-08-01 · confirmed 2026-08-01** |
| T-D-26 | **Boundary attribution pair** (one precedent — the straddling package block "belongs to the slice that opened it"): (a) the `calendar_days_actual` **boundary day** belongs to the slice covering that day's 00:00 UTC, and a period's slice fractions MUST sum to exactly 1 (fixture-asserted); (b) a **granule straddling a slice cut** belongs to the slice that opened it — `Q = sum of granule folds` stays additive (per-slice peak maxima never both count). | review #19/#17 → design/09 §4.3, design/03 §4.3, design/13 §4.1 | **adopted (autonomous) 2026-08-01 · confirmed 2026-08-01** |
| T-D-27 | **Pool observation effects + over-draw detection** (T-D-10 completion, joint with Contracts; **mirror still owed and its target now exists — 2026-08-17**: `gears/bss/contracts/docs/PRD.md` was authored on 2026-08-17 as the first Contracts artifact and greps **zero** for `T-D-27`, `CommitmentBalanceEffect`, over-draw and `balanceVersion`, so the debt this row records is open against a document that can now receive it rather than against a gear with no design set): a `CommitmentBalanceEffect` is published for **every outcome that observed a pool** — draw, refill, or **zero-draw + overage marker** (the effect stream is Contracts' only source of Rating unit identity, and T-D-10 obliges it to re-resolve overage-rated units it previously could not name); Contracts additionally detects **over-draw** at write-back (aggregate draws at one frozen `balanceVersion` exceeding the balance — the steady state under concurrent load; the §4.5 true-up is shortfall-only and cannot see it) and emits the same T-D-10 triggers. Cross-PRD: mirror in Contracts. | review #7/#8 → design/05 §4.1, design/15 §4.5, design/14 §4.5, design/11 §4.9 | **adopted (autonomous) 2026-08-01 · confirmed 2026-08-01 (joint — cross-PRD mirror owed)** |
| T-D-28 | **periodState close fence** (joint with Billing): transitions carry a per-`(subscription, period)` **sequence**; the slice-16 relay keeps highest-wins (a re-opened window is legitimate, so plain forward-only is wrong); correction routing re-reads the projection at route time and records `(periodState, sequence)` in the delta audit. Mis-routing across a close (wrong artifact kind + audit posture) was otherwise undetectable; double-billing was already excluded by dedup + Billing idempotency. | review #20/#48 → design/16 §3.6/§3.7, design/08 §4.3, design/14 §4.3 | **adopted (autonomous) 2026-08-01 · confirmed 2026-08-01 (joint — cross-PRD mirror owed)** |
| T-D-29 | **Adopt pricing D-113 — `usageCounterOnPlanChange`** (the owed Rating-side adoption): the **singular** plan-change tier-`Q` flag `∈ {reset (default) \| carry}`; the **target** plan's frozen flag governs; **absence = `reset`, never a rating failure** (supersedes design/09's "fail-closed when absent" and the plural "flags"); `carry` honoured per shared `(skuId, dimensionKey)` line **only** under the D-82/D-98/D-122 unit-field match across both frozen snapshots (else reset + operator signal — never a cross-denomination carry); **pricing publishes no pool flag** — pool carry stays Rating/Contracts-side, default reset. SEAMS row **P3** added. | pricing D-113; review #13 → design/09 ×5 sites, design/03 §4.3, design/05 §4.5, SEAMS P3, PRD §1.4/§6.11/§17.2 | **adopted 2026-08-01** (pricing D-113 CONFIRMED 2026-08-01 — the adoption is unqualified) |
| T-D-30 | **Adopt pricing D-78 — overlay line `cohort` filter** (the owed step-4 adoption): unset ⇒ `existing_grandfathered` rows exempt from the line (fail-safe default); a value ⇒ the line reaches only that generation; evaluated against the priced row's `cohort` axis before line selection. | pricing D-78; review #14 → design/04 §4.2 | **adopted 2026-08-01** |
| T-D-31 | **Adopt pricing D-114 — prefix-closed pin frontier**: a `CatalogVersion` is pin-eligible only once `CatalogVersionPublished` has fired, every subject row is warm-complete, **and every earlier version is itself pin-eligible** (monotonic frontier); matters gate-side wherever rating implements the eligibility check from its own text (design/14 §2.2). | pricing D-114; review #15 → design/11 §3.2/§3.6/§4.2, design/14 §2.2/§4.3 | **adopted 2026-08-01** |
| T-D-32 | **`usecase-finance-simulation` deferred to Follow-on**: no slice claims it, DESIGN §3.3 is a TBD skeleton, and evaluating a *candidate/draft* PriceWindow conflicts with the pin discipline (no draft read); the partner-overlay use case's "simulate against sample usage" step rides the same gate. A simulation surface, when wanted, is a named future increment with its own draft-read carve-out. | review #50 → DESIGN §3.3/§4, PRD §usecase | **adopted (autonomous) 2026-08-01 · confirmed 2026-08-01** |
| T-D-33 | **SB1 resolved — the subscriptions period fact is the recurring WHEN; the `PeriodTick` is fact-driven** (joint; the subscriptions side SUB-D-07/19/20/21 was ratified in its 2026-08-01 veto round): the synthesis trigger for **every period-driven unit** is consumption of the subscriptions period-fact set for `(subscription, billing period)` — `BillableItemCreated(kind=recurring)`, one fact per component interval — never the tick's own calendar. **`AnchorPeriod` ≡ the consumed fact's anchor-derived period identity** (the single period coordinate SB1's residue demanded; the tick-ledger key keeps its shape). Per-component recurring units **inherit the fact's key**; capacity-flavor charges and true-up surfacing ride the period's **plan-line fact** as their trigger, serialized on `(subscription, AnchorPeriod)` unchanged. The fact's carried fields are **absorbed**: suspended interval(s) + `pause_recurring \| continue` posture (the recurring line prorates over the un-suspended stretch under `pause_recurring`; `continue` bills the full period), the **period-start `payerTenantId`** and the `collectionPaused` marker (pass-through to the priced line — posting axes, no rating math). Zero-usage emission is now **by construction** (a fact arrives per component per period — subscriptions AC 5/27). A **missing** fact past the calendar boundary + grace horizon raises a detection alarm (§17.1 charge-coverage) — the tick never self-synthesizes; a **late** fact (subscriptions §4.3b revival, ≤ the 90-day dwell) synthesizes late, its as-of correctness the producer's (SUB-D-20). `AnchorCalendar` survives as **geometry** — usage window slicing under the frozen `billingAnchorPolicy` (D-20 clamp) — and as the watchdog clock, never a synthesis trigger; the joint K5 anchor fixture asserts calendar geometry ≡ fact period identity. | subscriptions SUB-D-07/19/20/21 (CONFIRMED 2026-08-01); design/14 §1.2/§3.1/§3.2/§3.6/§4.2, design/01 §4.2, design/09 §4.3, design/11 §4.3, PRD §9.2; SEAMS SB1 → RESOLVED | **adopted 2026-08-01 · confirmed 2026-08-01 (joint)** |
| T-D-34 | **`lineKey` value rule adopted verbatim from subscriptions SUB-D-21**: `plan#n` for the n-th `PlanLink` interval, `addon:{addOnId}#n` for the n-th interval of that add-on — `n` = the component's 1-based lifetime interval ordinal, assigned at interval open, immutable. Stability: a multi-period interval keeps its key; an in-place `changePlan` opens `plan#n+1` (a mid-period change ⇒ two period-driven units in that period, one per interval); a re-added add-on gets a fresh ordinal; `updateQuantity` opens no interval; a cancel+new successor is a new subscription; a sub-window slice never changes `lineKey` (T-D-12-consistent — the earlier definition already said so). **Bundle-component sub-lines**, where this gear materializes them, are **rating-internal sub-coordinates under the plan line's key** — the Billing handoff aggregates to the inherited fact key (AC 27; pricing `inst-bb-sum` sums component recurring into the bundle line), never a second fact-level key. Joint SUB-R6 fixture cases: plan + add-on period; add-on removed and re-added inside one period; in-place `changePlan` mid-period; a late §4.3b revival cut. | subscriptions SUB-D-21 (CONFIRMED 2026-08-01); design/01 §3.1 `lineKey`, design/14 §4.1; SEAMS SB1/§K note | **adopted 2026-08-01 · confirmed 2026-08-01 (joint fixture owed)** |
| T-D-35 | **K6 resolved — `SelectionKey` widened to the full ten-axis canonical key** (pricing D-196): the usage pair `(skuId, dimensionKey)` is a selection axis, taken from the evaluation line for `chargeKind = usage` and `none` otherwise (mirroring pricing's `COALESCE(meter,'')` index spelling); each priced usage line resolves its own row/window — design/03's "line within the selected row" reading retired; "at most one match" and non-overlap hold on the full ten. Alternative rejected: a rating-local line-projection contract over 8-axis selection — a second key definition is exactly what ADR-0001 exists to forbid. | SEAMS K6 → RESOLVED; ADR-0001 amendment; design/01 §1/§3/§5, design/02 (SelectionKey, FR row, uniqueness, step 4, seams), design/03 ADR row, design/04/05/06/07/09 ADR rows, design/README, design/11 | **adopted 2026-08-25 (K6 register + PR #14 review second)**  amended 2026-09-16: the row identity is `(skuId, dimensionKey)` (pricing D-372). |
| T-D-36 | **Pricing's charge-line split is adopted; three consumer rules change (pricing D-375, 2026-09-20 — contract-version break).** (1) `chargeKind ∈ {recurring, usage, one_time}` — exactly three; `one_time_setup` is gone and the pre-purchase decomposition returns `recurring` / `one_time` / flagged `usage`. (2) One-time charges stay **unrated**; their dedup, owned by Subscriptions, is per **phase-entry occurrence** `(tenantId, subscriptionId, phaseEntryId, componentOccurrenceId, chargeLineId)` and no longer per subscription lifetime (subscriptions SUB-D-28). (3) **Usage continuation key** = subscription + priced `skuId`/`meter`/`dimensionKey` + compatible aggregation-window identity: **no** phase-local `chargeLineId` / `lineVersionId` and **no** monetary-version id (`marketPriceId`, `priceId`) may reset a `Q` counter — a trial ending at 800 of a 1 000 allowance continues at 800, 250 more make 1 050, and 50 lie above the shared boundary. Incompatible quantity semantics are refused by pricing at publish (`PHASE_USAGE_INCOMPATIBLE`), so none reaches a transition; a **money** change never fails continuation. (4) A missing selected phase/market usage tariff answers **`USAGE_PRICE_MISSING`** — never an implicit free tariff, the previous phase's price, or another market's through FX; an explicit zero price accepts and accumulates. (5) The pinned snapshot now carries `chargeLineId`, `lineVersionId`, `marketPriceId` per price and `windowId`, `priceId` per window interval; step-2 selection resolves the interval's `priceId` rather than inferring it from lifecycle state. *Specified, not runtime-tested: this gear has no runtime in the workspace.* | PRD §5.2, pre-purchase contract; DESIGN one-time note; design/01 §Domain; design/14 §4.1; SEAMS K3; counterpart pricing `design/06-consumer-contracts.md` `inst-pe-*`, `inst-uc-*` | **adopted 2026-09-20** |
| T-D-37 | **The pricing contract is replaced by the PriceBook model.** The eight-axis key (later extended to ten), phases, cohort, grandfatherUntil and the catalog-version contract are superseded. Rating adapts in its own plan against `GET /bss-pricing/v1/resolve` and `GET /bss-pricing/v1/prices/{id}` (the read contract, pricing D-419–D-422 — amendment below): per-book SKU entries, per-value chains, renewal pins and versioned descriptors replace the legacy selection/publication model. Historical citations marked with this decision are not active dependencies. | PriceBook spec §7.1, §12 (`docs/superpowers/specs/2026-09-24-pricebook-model-design.md`); pricing D-384–D-415, D-419–D-422 | **DECIDED 2026-09-25; adapter implementation deferred to Rating's plan** |
| T-D-38 | **Rating owns the minimum-fee floor (pricing D-388): per price, per subscription, per billing period, after included quantities and before promotions, prorated by coverage — with pricing's acceptance example; pricing builds no quote (D-415).** Two values rated 10 + 10 on one default price with min_fee 30 bill 30; two own prices with min_fee 30 each bill 60. Pricing stores and validates min_fee and returns it on every binding of `GET /bss-pricing/v1/resolve` (pricing D-420, D-421 — amendment below). | pricing D-388, D-415 (`gears/bss/pricing/docs/DECISIONS.md`); PriceBook spec §2 decision 13 | **DECIDED 2026-09-25** |

## Open items

> *Closed 2026-07-28 (verified against [`SEAMS.md`](./SEAMS.md) — this list had lagged the seam register):*
> **O3** — pricing confirmed "most-specific-wins" is a tie-break, not overlay exclusivity (pricing `design/09`
> `inst-plv-class-tiebreak` states stack-all normatively; SEAMS O3 row "fully closed").
> **M6** — dimensional launch-posture wording landed on both sides (pricing `design/03` §6 `dimension_key`
> note + rating `design/03` §4.2; SEAMS M6 row "closed").
> **Vendored-PRD AC numbering drift** — fixed in the PRD (no `AC 21+` cross-reference remains; §5 scope table
> and FR rationales cite the actual §12 numbers, slice 05/06 traceability updated).

- **NFR ratification (Design-lock blocker)** — §7.1 targets are still working assumptions pending the program NFR workshop (p95 ≤ 100 ms catalog lookup, p95 < 1 s rating path, ≥ 10M events/day/region, DECIMAL precision with Billing). PRD §16 names committing them a Design-lock precondition; the sibling pricing gear ratified its NFRs 2026-07-28, this gear's are outstanding.
- **Plan-scoped coupon evaluation base (T-D-22)** — `line_total` fails closed until the plan-period assembly point, its trigger, and its cascade behaviour are pinned jointly with Promotions.
- **From the 2026-07-11 slice review** (details: [`SEAMS.md`](./SEAMS.md) technical residues): composite × dimensions input-join rule (design/03 §3.6); mixed-settlement coupon rules — fail-closed meanwhile (design/06 §4.2); seat-change boundary transport with Subscriptions (design/09 §4.3); bundle-level coupon attachment (design/10 §4.5). Foundation opens: DECIMAL precision (with Billing), clamp-vs-credit (PRD §15). *Resolved 2026-07-11:* S1 segment naming → T-D-09; delta-dedup owner → T-D-11; balance write-back → T-D-10.
- **From the 2026-07-11 tri-review** (to close before Design lock): coupon equal-benefit tie-break (design/06 §4.2 — default proposal: ascending `couponId`); `whole_unit` attribution on a split (design/09 §4.1); validator-4 enforcement point — Contracts-side publish gate (design/10 §4.4); validator packaging — shared crate vs pricing-side impl (design/10); RatedCharge/BillableItem mapping ratification by the pipeline design (design/11 §4.1 → pipeline slice 15 — intra-gear since T-D-16).
- **Registry seams (`products` gear — PRD vendored 2026-07-16 to `gears/bss/products/docs/PRD.md`, from PR #4177):** RG1 freeze-protocol composition (registry `freezeComplete` × pricing pin/warm-completion × rating W2 replay — rating is not in the freeze-participant set) — **still open**; RG2 single-unit-per-SKU vs dimensional lines — **closed** (fix applied in the vendored copy); RG3 pre-consolidation naming — localization debt recorded in the vendored PRD's provenance note. Tracked in [`SEAMS.md`](./SEAMS.md) §I.
- **Migration residues (ADR-0002 / T-D-16):** prose re-voicing of the evaluation-core slices (historical "Tariffs" → the core, neighbour-"Rating" → the pipeline) — bridge notes in [`DESIGN.md`](./DESIGN.md) / [`SEAMS.md`](./SEAMS.md) apply meanwhile; the `Tariffs`/`PLAL` → `Rating`/`rating-core` prose rename is **done in the living docs** (design slices 01–16, DESIGN.md, README, PRD.md — *Done 2026-07-16; corrected 2026-07-28*: the mechanical pass had collapsed sentences that *contrast* the two halves into "Rating…Rating" — the determinism FR, §17.1 step 3, contract directions, the §2.1 deprecated-names row, and the DESIGN/README terminology bridges — all re-voiced to rating-core / rating pipeline in the 2026-07-28 review wave); this register, [`SEAMS.md`](./SEAMS.md), and the ADRs deliberately retain the historical "Tariffs"/"PLAL" naming as dated decision/analysis records (each carries a bridge note). The **pricing-side Commit C** ("Tariffs" prose in `gears/bss/pricing/docs`) remains the one open cross-gear rename. (Upstream vhp-architecture is legacy provenance, not maintained — no propagation obligation for the manifest §4.2 note or the `PRD-rating-engine-202604031200` marker.) *Done 2026-07-11:* artifacts-board `/rating` alias (page retitled; `/tariffs` kept as an alias). *Done 2026-07-15:* pipeline slices 12–16 authored beyond skeleton (full §1–§5); the slice-11 §4.1 handoff mapping ratified intra-gear by slice 15 (finding C6 closed).

## Source

Full seam analysis, rationale, ownership matrix, and rejected alternatives are in
[`SEAMS.md`](./SEAMS.md) (4-agent cross-gear pass, 2026-07-10). Requirements in
[`PRD.md`](./PRD.md).


**Pricing D-372 amendment (2026-09-16), T-D-35:** row selection uses `(skuId, dimensionKey)`. The SKU is the priced row's registry resource identity; `meter` is derived and frozen for measurement-model mapping, and two resource SKUs may share it. The plan-level SKU continues to name the sold offer.

**Pricing D-419–D-422, D-424 and D-425 amendment (2026-09-26), T-D-37/T-D-38:** the read contract Rating adapts against is built and frozen. `GET /bss-pricing/v1/resolve?plan_revision_id=&date=&item_id=&pins=` (D-419) answers a `published` or `superseded` revision on one date, the period start; `item_id` narrows the answer to one item; `pins` are the subscription's current bindings, comma-separated, each `<price_id>` (the price a chain is bound to) or `<price_id>:<dim_value>` (a default-chain price a value was bound to), at most 1 000 per request. Refusals: 400 `QUERY_INVALID`, `DATE_INVALID`, `PIN_FOREIGN` (a pin that does not parse, that is not an approved price of the tenant on an entry the revision names, or that pins a value to a price outside the default chain refuses the whole request), `PIN_DUPLICATE`, `PINS_TOO_MANY`; 404 for an unknown or another tenant's revision and for an item the revision lacks; 409 `REVISION_NOT_PUBLISHED`; Products' own refusal as Products gave it; 503 `REGISTRY_UNAVAILABLE`. Per item the answer is the whole matrix (D-420) — the default chain, every value registered today and any value a pin names — and per chain either a binding or `uncovered: true`, never a refusal and never an invented price: a signup binds the price in force (the value's own chain, else the default; `dim_used` says which); a renewal walks the pinned chain through `all` successors and stops before the first `new` one (`pinned_from` names the pin); a binding is always a price in force, so a walk that ends on an ended price binds as a signup would; a value bound to the default moves to its own later `all` price. A binding carries `price_id`, `dim_used`, `pinned_from`, `model`, `price` (the model's money object as stored — `amount`, `rate`, `tiers` of `up_to` and `rate`, or `package_size` and `package_price` — every amount exact decimal text), `min_fee`, `eligibility`, `effective_from`, `effective_to`, `temporary_until`, `ends_on` and `keep_for_bound`; the item carries `sku_version` as of the date (`published_version`, `effective_from`; null when Products has no version then or does not know the SKU), `meter` (`usage_type_ref`, `unit`) and the invoice inputs `invoice_line_template`, `gl_code`, `tax_category` and `billing_timing`, each `{ value, source }` with source `entry`, `sku`, `tenant` or null (D-421); the revision carries `currency`, `currency_minor_digits` and `rounding_policy`. Rating slices a period at a binding's `ends_on`, the price's own end (a temporary price's end or an explicit close), never at `effective_to`, the stored window, which the start of a `new` successor the pin did not take also sets (pricing D-425). No totals and no promotion (pricing D-409, D-415). A replay reads the pinned money at `GET /bss-pricing/v1/prices/{id}` (D-422), served forever for an approved price whatever its window; any other id is one and the same 404. The frozen answers are pricing's golden consumer contracts, `gears/bss/pricing/pricing/tests/contract/*.json` — thirteen files, the same answers on SQLite and Postgres — and Rating's adapter tests read them. A Rating caller, a system subject, needs only pricing `plan:read` (resolve) and `price:read` (the pinned read): resolve reads each SKU version as pricing's system actor (pricing D-424). T-D-38 stands as decided: the minimum-fee floor and proration are Rating's arithmetic over `min_fee` and the bound windows, and pricing computes neither. The legacy joint fixture corpus, its proration family included, was deleted with `gears/bss/fixtures/` in pricing phase 4, so Rating's proration and floor cases are Rating's to author.

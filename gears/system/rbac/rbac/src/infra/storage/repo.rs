//! SeaORM-backed repository implementations for the traits defined in
//! `domain/repos/`.

// `pub` so integration tests can construct the repos directly.
pub mod role_assignment_repo;
pub mod role_definition_repo;
